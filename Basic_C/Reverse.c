#include <stdio.h>
#include<stdio.h>

int main()
{
    int n,originalN,reversedN=0,remainder;
    printf("Enter a number\n");
    scanf("%d", &n);
    remainder=n%10;
    printf("%d", remainder);
  return 0;
}
